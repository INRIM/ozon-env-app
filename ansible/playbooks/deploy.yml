---
- name: Deploy ozon-env-app stack (setup-db + bootstrap)
  hosts: ozonapp
  gather_facts: false
  vars:
    ozonapp_action: deploy
    ozonapp_repo_root: "{{ playbook_dir | dirname | dirname }}"
  roles:
    - role: ozonapp_deploy

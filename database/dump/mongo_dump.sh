#!/usr/bin/env bash
# Example commands for manual backup/restore.
# Replace placeholders before execution.
#
# mongodump -u USER -p PASSWORD -d DBNAME --out dump
# mongorestore -u USER -p PASSWORD -d DBNAME /dump/DBNAME

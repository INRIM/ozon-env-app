# Env specifico di identity_manager (override del .env base dello stack).
# Generato/gestito da ./run.sh come `service.env` (gitignored).
# Copia in service.env solo se vuoi configurare a mano.

# --- Opzionali (default nel codice) ---
# Intervallo in minuti tra ogni ciclo di allineamento (default 10)
#IDENTITY_MANAGER_INTERVAL_MINUTES=10
